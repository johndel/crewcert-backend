<!DOCTYPE html>
<html>
<head>
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background: #0d6efd; color: white; padding: 20px; text-align: center; }
    .content { padding: 20px; background: #f8f9fa; }
    .button { display: inline-block; background: #0d6efd; color: white; padding: 12px 24px; text-decoration: none; border-radius: 4px; }
    .certificate-list { background: white; padding: 15px; border-radius: 4px; margin: 15px 0; }
    .footer { text-align: center; padding: 20px; color: #6c757d; font-size: 12px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1 style="margin: 0;">CrewCert</h1>
    </div>

    <div class="content">
      <h2>Hello <%= @crew_member.first_name %>,</h2>

      <p>
        You are requested to upload your certificates for the vessel <strong><%= @vessel.name %></strong>
        in your role as <strong><%= @role.name %></strong>.
      </p>

      <% if @required_certificates.any? %>
        <div class="certificate-list">
          <strong>Required Certificates:</strong>
          <ul>
            <% @required_certificates.each do |cert_name| %>
              <li><%= cert_name %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <p>Please click the button below to access your upload page:</p>

      <p style="text-align: center;">
        <a href="<%= @upload_url %>" class="button">Upload Certificates</a>
      </p>

      <p style="color: #6c757d; font-size: 14px;">
        This link will expire on <strong><%= @certificate_request.expires_at.strftime("%B %d, %Y") %></strong>.
      </p>

      <p>
        If you have any questions, please contact your fleet administrator.
      </p>

      <p>
        Best regards,<br>
        The CrewCert Team
      </p>
    </div>

    <div class="footer">
      <p>This is an automated message from CrewCert. Please do not reply to this email.</p>
      <p>
        If the button above doesn't work, copy and paste this link into your browser:<br>
        <a href="<%= @upload_url %>"><%= @upload_url %></a>
      </p>
    </div>
  </div>
</body>
</html>
